module.exports = {
  theme: {
    // Replaces default settings or add new settings
    container: {
      center: true,
    },
  },
  variants: {
    backgroundColor: ['hover', 'focus', 'active'],
  },
};
